{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project",
            "type": "shell",
            "command": "make", // or "ninja" / "cmake --build build"
            "group": "build"
        }
    ]
}